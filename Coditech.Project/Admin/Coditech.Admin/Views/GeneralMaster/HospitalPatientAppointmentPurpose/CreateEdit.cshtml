@model HospitalPatientAppointmentPurposeViewModel
@{
    ViewBag.Title = Model.HospitalPatientAppointmentPurposeId > 0 ? "Update Appointment Purpose" : "Add Appointment Purpose";
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
    string actionName = ViewContext.RouteData.Values["action"].ToString();
}
<!-- Page Content-->
<div class="page-content mt-5">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <div class="row align-items-center">
                            <div class="col">
                                @if (Model.HospitalPatientAppointmentPurposeId > 0)
                                {
                                    <h4 class="card-title">Update Appointment Purpose</h4>
                                }
                                else
                                {
                                    <h4 class="card-title">Create Appointment Purpose</h4>
                                }
                            </div><!--end col-->
                        </div>  <!--end row-->
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm(actionName, controllerName, FormMethod.Post))
                        {
                            @Html.HiddenFor(m => m.HospitalPatientAppointmentPurposeId)
                            <div class="row">
                                <div class="col-lg-4">
                                    <div class="mb-3">
                                        @Html.LabelFor(x => x.HospitalPatientAppointmentPurposeName, new { @class = "form-label required" })
                                        @Html.TextBoxFor(x => x.HospitalPatientAppointmentPurposeName, new { @class = "form-control" })
                                        @Html.ValidationMessageFor(x => x.HospitalPatientAppointmentPurposeName)
                                    </div>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-success">@AdminResources.LabelSave</button>
                            <a href="../@controllerName/List" class="btn btn-secondary"><i class="ft-x"></i>@AdminResources.LabelCancel</a>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
